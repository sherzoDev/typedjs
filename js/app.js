let options = {
    strings: ["O'zbekiston", "Ibrohim", "Parij"],
    typeSpeed: 40,
    typeSpeed: 60,
    startDelay: 000,
    backSpeed: 60,
    loop: true,
    loopCount: Infinity,
};

let typed = new Typed('.type', options);